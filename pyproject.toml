[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lazyburn"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "click",
    "rich",
]

[project.scripts]
lazyburn = "lazyburn:cli"

[tool.setuptools]
py-modules = ["lazyburn"]
